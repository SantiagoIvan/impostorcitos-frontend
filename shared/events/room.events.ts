export enum RoomEvents {
    LIST = "room:list",
    CREATE = "room:create",
    CREATED = "room:created",
    JOIN = "room:join",
    JOINED = "room:joined",
    LEAVE = "room:leave",
    USER_LEFT = "room:user_left"
}
